/** Identifiers that must match package.json ("name") and config.schema.json ("name"). */
export const PLATFORM_NAME = 'MyHonda';
export const PLUGIN_NAME = 'homebridge-myhonda';

export const DEFAULT_POLL_INTERVAL_SECONDS = 300;
export const MIN_POLL_INTERVAL_SECONDS = 60;
